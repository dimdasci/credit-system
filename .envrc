# Load project environment variables with direnv
# Put secrets in `.env` or `.env.local` (both are gitignored)

# Load base env vars
dotenv_if_exists .env

# Optionally override with developer-specific values
dotenv_if_exists .env.local

# Example: ensure your GitHub PAT is set in one of the files above
# GITHUB_PERSONAL_ACCESS_TOKEN=...
